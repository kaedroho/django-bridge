# Django Bridge Test Project

This folder contains a test project for checking the behaviour of Django Bridge during development.

It's designed to only work with docker-compose, to set it up run:

```shell
make setup
```

To start, call the relevant start command for the framework you are testing against

```shell
make start-react
make start-vue
```
